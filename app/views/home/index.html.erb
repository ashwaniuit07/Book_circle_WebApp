<div data-controller="hello" class="relative w-full min-h-screen flex flex-col items-center justify-center text-center
            bg-gradient-to-r from-black via-gray-500 to-white">

  <!-- App Description -->
  <div class="bg-gray-900 bg-opacity-70 backdrop-blur-lg p-8 rounded-2xl shadow-lg mb-12 max-w-xl">
    <h1 class="text-4xl md:text-5xl font-bold text-white mb-4 drop-shadow-lg">Welcome to BookCircle 📚</h1>
    <p class="text-gray-200 text-lg drop-shadow-md">
      BookCircle is a modern platform connecting students and schools to buy, sell, and share books easily.
      Choose your category below to get started.
    </p>
  </div>

  <!-- Step 1: Main Selection Boxes -->
  <div data-hello-target="mainBoxes" class="grid grid-cols-1 sm:grid-cols-2 gap-8 w-full max-w-4xl transition-all duration-500 ease-in-out">
    <!-- Public User -->
    <%= link_to new_user_registration_path, class: "cursor-pointer bg-gray-800 text-white rounded-2xl p-12 shadow-lg hover:shadow-2xl transition duration-300 transform hover:scale-105" do %>
      <h2 class="text-2xl font-bold mb-2">Public User</h2>
      <p>Browse and purchase books from the community.</p>
    <% end %>

    <!-- School / Student -->
    <div data-action="click->hello#showSchoolOptions" class="cursor-pointer bg-gray-800 text-white rounded-2xl p-12 shadow-lg hover:shadow-2xl transition duration-300 transform hover:scale-105">
      <h2 class="text-2xl font-bold mb-2">School / Student</h2>
      <p>Login as school admin or student to manage and share books.</p>
    </div>
  </div>

  <!-- Step 2: School Options -->
  <div data-hello-target="schoolOptions" class="mt-8 hidden grid grid-cols-1 sm:grid-cols-2 gap-8 w-full max-w-4xl transition-all duration-500 ease-in-out">
    <!-- Student -->
    <%= link_to new_user_registration_path, class: "cursor-pointer bg-gray-700 text-white rounded-2xl p-12 shadow-lg hover:shadow-2xl transition duration-300 transform hover:scale-105" do %>
      <h2 class="text-2xl font-bold mb-2">Student</h2>
      <p>Signup or login as a student.</p>
    <% end %>

    <!-- School Admin -->
    <div data-action="click->hello#showSchoolAdminForm" class="cursor-pointer bg-gray-700 text-white rounded-2xl p-12 shadow-lg hover:shadow-2xl transition duration-300 transform hover:scale-105">
      <h2 class="text-2xl font-bold mb-2">School Admin</h2>
      <p>Signup or manage your school admin profile.</p>
    </div>
  </div>

  <!-- Step 3: School Admin Signup Form -->
  <div data-hello-target="schoolAdminForm" class="mt-8 hidden transition-all duration-500 ease-in-out w-full max-w-2xl relative">
    <div class="bg-gray-800 bg-opacity-80 p-8 rounded-2xl shadow-lg">
      
      <!-- Top-right Cross Icon -->
      <button data-action="click->hello#backToSchoolOptions" class="absolute top-3 right-3 text-white text-xl font-bold hover:text-gray-300">✕</button>

      <h2 class="text-3xl font-bold text-white mb-6 text-center">School Admin Profile Form</h2>

      <%= form_with url: school_admins_path, method: :post, local: true do |f| %>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">

          <!-- Official Email ID -->
          <div class="flex flex-col sm:col-span-2">
            <%= f.label :official_email_id, "Official Email ID", class: "text-white mb-1" %>
            <%= f.email_field :official_email_id, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500 w-full" %>
          </div>

          <!-- Password -->
          <div class="flex flex-col sm:col-span-2">
            <%= f.label :password, "Password", class: "text-white mb-1" %>
            <%= f.password_field :password, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500 w-full" %>
          </div>

          <!-- Confirm Password -->
          <div class="flex flex-col sm:col-span-2">
            <%= f.label :password_confirmation, "Confirm Password", class: "text-white mb-1" %>
            <%= f.password_field :password_confirmation, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500 w-full" %>
          </div>

          <!-- UDISE Code -->
          <div class="flex flex-col">
            <%= f.label :udise_code, "UDISE Code", class: "text-white mb-1" %>
            <%= f.text_field :udise_code, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500" %>
          </div>

          <!-- School Affiliation Code -->
          <div class="flex flex-col">
            <%= f.label :school_affiliation_code, "School Affiliation Code", class: "text-white mb-1" %>
            <%= f.text_field :school_affiliation_code, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500" %>
          </div>

          <!-- School Name -->
          <div class="flex flex-col sm:col-span-2">
            <%= f.label :school_name, "School Name", class: "text-white mb-1" %>
            <%= f.text_field :school_name, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500 w-full" %>
          </div>

          <!-- City -->
          <div class="flex flex-col">
            <%= f.label :city, "City", class: "text-white mb-1" %>
            <%= f.text_field :city, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500" %>
          </div>

          <!-- Location -->
          <div class="flex flex-col">
            <%= f.label :location, "Location", class: "text-white mb-1" %>
            <%= f.text_field :location, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500" %>
          </div>

          <!-- State -->
          <div class="flex flex-col sm:col-span-2">
            <%= f.label :state, "State", class: "text-white mb-1" %>
            <%= f.text_field :state, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500 w-full" %>
          </div>

        </div>

        <div class="mt-6 text-center">
          <%= f.submit "Sign Up", class: "px-8 py-3 bg-white text-gray-800 font-semibold rounded-2xl hover:bg-gray-200 transition duration-300" %>
        </div>
      <% end %>

      <p class="text-sm text-gray-300 mt-4 text-center">
        Already have an account? 
        <a href="#" class="text-blue-500 underline" data-action="click->hello#showSchoolAdminLogin">Login</a>
      </p>
    </div>
  </div>

  <!-- Step 4: School Admin Login Form -->
  <div data-hello-target="schoolAdminLogin" class="mt-8 hidden transition-all duration-500 ease-in-out w-full max-w-2xl relative">
    <div class="bg-gray-800 bg-opacity-80 p-8 rounded-2xl shadow-lg">
      
      <!-- Top-right Cross Icon -->
      <button data-action="click->hello#backToSchoolOptions" class="absolute top-3 right-3 text-white text-xl font-bold hover:text-gray-300">✕</button>

      <h2 class="text-3xl font-bold text-white mb-6 text-center">School Admin Login</h2>

      <%= form_with url: school_admin_login_path, method: :post, local: true do |f| %>
        <div class="grid grid-cols-1 gap-6">
          <!-- Official Email ID -->
          <div class="flex flex-col">
            <%= f.label :official_email_id, "Official Email ID", class: "text-white mb-1" %>
            <%= f.email_field :official_email_id, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500" %>
          </div>

          <!-- Password -->
          <div class="flex flex-col">
            <%= f.label :password, "Password", class: "text-white mb-1" %>
            <%= f.password_field :password, class: "p-3 rounded-lg border border-gray-600 bg-gray-900 text-white focus:outline-none focus:ring-2 focus:ring-gray-500" %>
          </div>
        </div>

        <div class="mt-6 text-center">
          <%= f.submit "Login", class: "px-8 py-3 bg-white text-gray-800 font-semibold rounded-2xl hover:bg-gray-200 transition duration-300" %>
        </div>
      <% end %>

      <p class="text-sm text-gray-300 mt-4 text-center">
        Don't have an account? 
        <a href="#" class="text-blue-500 underline" data-action="click->hello#showSchoolAdminForm">Sign Up</a>
      </p>

    </div>
  </div>

</div>
